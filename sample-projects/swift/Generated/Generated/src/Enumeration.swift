/**
 * Enumeration.swift
 *
 * Generated by JSON Model Generator v0.0.4 on Aug 13, 2016
 * https://github.com/intere/generator-json-models
 *
 * The generator tool is licensed under the LGPL: http://www.gnu.org/licenses/lgpl-3.0.html#content
 */

import Foundation

class Enumeration {
    var purseTypes: [EnumerationPurseTypes]?
    var skillLevels: [EnumerationSkillLevels]?
    var tournamentSizes: [EnumerationTournamentSizes]?
    var tournamentStates: [EnumerationTournamentStates]?
    var tournamentTypes: [EnumerationTournamentTypes]?
    var userTypes: [EnumerationUserTypes]?

}

// MARK: - Serialization

extension Enumeration {

    /**
    Serializes this Enumeration to a Dictionary.
    - Returns: A Map which represents this object as a serialized object
    */
    func toMap() -> [String: AnyObject] {
        var dict = [String: AnyObject]()

        dict["purseTypes"] =  EnumerationPurseTypes.toMapArray(purseTypes)
        dict["skillLevels"] =  EnumerationSkillLevels.toMapArray(skillLevels)
        dict["tournamentSizes"] =  EnumerationTournamentSizes.toMapArray(tournamentSizes)
        dict["tournamentStates"] =  EnumerationTournamentStates.toMapArray(tournamentStates)
        dict["tournamentTypes"] =  EnumerationTournamentTypes.toMapArray(tournamentTypes)
        dict["userTypes"] =  EnumerationUserTypes.toMapArray(userTypes)

        return dict
    }

    /**
    Converts the provided array of Enumeration Objects to an array of dictionaries.
    - Parameter models: An array of Enumeration Objects to be converted to an array of dictionaries.
    - Returns: An array of Dictionaries
    */
    class func toMapArray(models: [Enumeration]?) -> [[String: AnyObject]]? {
        guard let models = models else {
            return nil
        }
        var maps = [[String: AnyObject]]()

        for model in models {
            maps.append(model.toMap())
        }

        return maps
    }

    /**
    Converts this model to JSON Data.
    - Returns: Optional JSON Data (returns nil if there's a problem serializing the map).
    */
    func toJsonData() -> NSData? {
        let dict = toMap()
        do {
            return try NSJSONSerialization.dataWithJSONObject(dict, options: .PrettyPrinted)
        } catch {
            print("Error serializing this model to JSON: \(dict)")
        }

        return nil
    }

	/**
	Converts the provided array of models to JSON Data.
	- Parameter array: Array of Enumeration objects to be serialized.
	- Returns: NSData that contains the serialized objects, or nil if there's a problem serializing them.
	*/
	class func toJsonData(array: [Enumeration]) -> NSData? {
		let mapArray = toMapArray(array) as! AnyObject
		do {
			return try NSJSONSerialization.dataWithJSONObject(mapArray, options: .PrettyPrinted)
		} catch {
			print("Error serializing array of models to JSON: \(mapArray)")
		}
		return nil
	}

	/**
	Converts this model to a JSON String.
	- Returns: Optional String (nil if there's a problem serializing the map to Data or converting it to a UTF-8 String).
	*/
	func toJsonString() -> String? {
	    guard let data = toJsonData() else {
	        return nil
        }

        return NSString(data: data, encoding: NSUTF8StringEncoding) as? String
	}

    /**
    Converts the provided array of models to a JSON String.
    - Parameter array: Array of Enumeration objects to be serialized
    - Returns: Optional String (nil if there's a problem serializing to NSData or converting to a UTF-8 String).
    */
    class func toJsonString(array: [Enumeration]) -> String? {
        guard let data = toJsonData(array) else {
            return nil
        }

        return NSString(data: data, encoding: NSUTF8StringEncoding) as? String
    }

}

// MARK: - Deserialization

extension Enumeration {

    /**
    Gets you a Enumeration object from the provided map
    - Parameter map: The Map to read the data from.
    - Returns: A Enumeration object (assuming the map you provided is non-nil)
    */
    class func fromMap(map: [String:AnyObject]?) -> Enumeration? {
        guard let map = map else {
            return nil
        }

        let model = Enumeration()
        model.purseTypes = EnumerationPurseTypes.fromArrayOfMaps(map["purseTypes"] as? [[String: AnyObject]])
        model.skillLevels = EnumerationSkillLevels.fromArrayOfMaps(map["skillLevels"] as? [[String: AnyObject]])
        model.tournamentSizes = EnumerationTournamentSizes.fromArrayOfMaps(map["tournamentSizes"] as? [[String: AnyObject]])
        model.tournamentStates = EnumerationTournamentStates.fromArrayOfMaps(map["tournamentStates"] as? [[String: AnyObject]])
        model.tournamentTypes = EnumerationTournamentTypes.fromArrayOfMaps(map["tournamentTypes"] as? [[String: AnyObject]])
        model.userTypes = EnumerationUserTypes.fromArrayOfMaps(map["userTypes"] as? [[String: AnyObject]])
        return model
    }

    /**
    Creates an Array of Enumeration objects for the provided Array of Maps.
    - Parameter mapArray: An array of Maps, assumes that each map is a serialized Enumeration.
    - Returns: An array of Enumeration objects (should be 1 to 1).
    */
    class func fromArrayOfMaps(mapArray: [[String:AnyObject]]?) -> [Enumeration]? {
        guard let mapArray = mapArray else {
            return nil
        }

        var modelArray = [Enumeration]()

        for map in mapArray {
            guard let model = fromMap(map) else {
                continue
            }
            modelArray.append(model)
        }

        return modelArray
    }

    /**
    Gives you back a collection of Enumeration objects for the provided JSON Serialized Data.  This method handles both a single object (map) and an array of objects (array of maps).
    - Parameter data: The NSData (optional) to pull the objects from.
    - Returns: An array of Enumeration objects that were deserialized from the provided data.
    */
    class func fromData(data: NSData?) -> [Enumeration]? {
        guard let data = data else {
            return []
        }

        do {
            let jsonData = try NSJSONSerialization.JSONObjectWithData(data, options: .AllowFragments)

            // If we have an array of dictionaries:
            if let jsonArray = jsonData as? [[String:AnyObject]] {
                return fromArrayOfMaps(jsonArray)
            } else if let jsonMap = jsonData as? [String:AnyObject] {
                if let model = fromMap(jsonMap) {
                    return [model]
                }
            } else {
                print("Unknown JSON Data type: \(jsonData)")
            }
        } catch {
            print("Error deserializing json data")
        }

        return []
    }

	/**
	 Gives you back a collection of Enumeration objects after deserializing them from a String to Data, Data to a Dictionary and then finally to a Model Object.
	 - Parameter json: The (UTF-8) JSON String to deserialize into Enumeration objects.
	 - Returns: An Array of Enumeration objects.
	*/
	class func fromJson(json: String) -> [Enumeration]? {
		let data = (json as NSString).dataUsingEncoding(NSUTF8StringEncoding)
		return fromData(data)
	}

}

