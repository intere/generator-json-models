/**
 * User.swift
 *
 * Generated by JSON Model Generator v0.0.5 on Aug 28, 2016
 * https://github.com/intere/generator-json-models
 *
 * The generator tool is licensed under the LGPL: http://www.gnu.org/licenses/lgpl-3.0.html#content
 */

import Foundation

class User {
    var accountCreationDate: NSDate?
    var admin: Bool?
    var age: Int?
    var email: String?
    var emailUser: Bool?
    var facebookId: String?
    var facebookUser: Bool?
    var theId: String?
    var lastLoggedIn: NSDate?
    var name: String?
    var prototype: Bool?
    var skillLevel: Int?
    var type: Int?
    var username: String?
    var weightInKg: Int?

}

// MARK: - Serialization

extension User {

    /**
    Serializes this User to a Dictionary.
    - Returns: A Map which represents this object as a serialized object
    */
    func toMap() -> [String: AnyObject] {
        var dict = [String: AnyObject]()

        dict["accountCreationDate"] = User.toEpochDateInt(accountCreationDate)
        dict["admin"] = admin
        dict["age"] = age
        dict["email"] = email
        dict["emailUser"] = emailUser
        dict["facebookId"] = facebookId
        dict["facebookUser"] = facebookUser
        dict["id"] = theId
        dict["lastLoggedIn"] = User.toEpochDateInt(lastLoggedIn)
        dict["name"] = name
        dict["prototype"] = prototype
        dict["skillLevel"] = skillLevel
        dict["type"] = type
        dict["username"] = username
        dict["weightInKg"] = weightInKg

        return dict
    }

    /**
    Converts the provided array of User Objects to an array of dictionaries.
    - Parameter models: An array of User Objects to be converted to an array of dictionaries.
    - Returns: An array of Dictionaries
    */
    class func toMapArray(models: [User]?) -> [[String: AnyObject]]? {
        guard let models = models else {
            return nil
        }
        var maps = [[String: AnyObject]]()

        for model in models {
            maps.append(model.toMap())
        }

        return maps
    }

    /**
    Converts this model to JSON Data.
    - Returns: Optional JSON Data (returns nil if there's a problem serializing the map).
    */
    func toJsonData() -> NSData? {
        let dict = toMap()
        do {
            return try NSJSONSerialization.dataWithJSONObject(dict, options: .PrettyPrinted)
        } catch {
            print("Error serializing this model to JSON: \(dict)")
        }

        return nil
    }

	/**
	Converts the provided array of models to JSON Data.
	- Parameter array: Array of User objects to be serialized.
	- Returns: NSData that contains the serialized objects, or nil if there's a problem serializing them.
	*/
	class func toJsonData(array: [User]) -> NSData? {
		let mapArray = toMapArray(array) as! AnyObject
		do {
			return try NSJSONSerialization.dataWithJSONObject(mapArray, options: .PrettyPrinted)
		} catch {
			print("Error serializing array of models to JSON: \(mapArray)")
		}
		return nil
	}

	/**
	Converts this model to a JSON String.
	- Returns: Optional String (nil if there's a problem serializing the map to Data or converting it to a UTF-8 String).
	*/
	func toJsonString() -> String? {
	    guard let data = toJsonData() else {
	        return nil
        }

        return NSString(data: data, encoding: NSUTF8StringEncoding) as? String
	}

    /**
    Converts the provided array of models to a JSON String.
    - Parameter array: Array of User objects to be serialized
    - Returns: Optional String (nil if there's a problem serializing to NSData or converting to a UTF-8 String).
    */
    class func toJsonString(array: [User]) -> String? {
        guard let data = toJsonData(array) else {
            return nil
        }

        return NSString(data: data, encoding: NSUTF8StringEncoding) as? String
    }

}

// MARK: - Deserialization

extension User {

    /**
    Gets you a User object from the provided map
    - Parameter map: The Map to read the data from.
    - Returns: A User object (assuming the map you provided is non-nil)
    */
    class func fromMap(map: [String:AnyObject]?) -> User? {
        guard let map = map else {
            return nil
        }

        let model = User()
        model.accountCreationDate = User.fromEpochDateInt(map["accountCreationDate"] as? Int)
        model.admin = map["admin"] as? Bool
        model.age = map["age"] as? Int
        model.email = map["email"] as? String
        model.emailUser = map["emailUser"] as? Bool
        model.facebookId = map["facebookId"] as? String
        model.facebookUser = map["facebookUser"] as? Bool
        model.theId = map["id"] as? String
        model.lastLoggedIn = User.fromEpochDateInt(map["lastLoggedIn"] as? Int)
        model.name = map["name"] as? String
        model.prototype = map["prototype"] as? Bool
        model.skillLevel = map["skillLevel"] as? Int
        model.type = map["type"] as? Int
        model.username = map["username"] as? String
        model.weightInKg = map["weightInKg"] as? Int
        return model
    }

    /**
    Creates an Array of User objects for the provided Array of Maps.
    - Parameter mapArray: An array of Maps, assumes that each map is a serialized User.
    - Returns: An array of User objects (should be 1 to 1).
    */
    class func fromArrayOfMaps(mapArray: [[String:AnyObject]]?) -> [User]? {
        guard let mapArray = mapArray else {
            return nil
        }

        var modelArray = [User]()

        for map in mapArray {
            guard let model = fromMap(map) else {
                continue
            }
            modelArray.append(model)
        }

        return modelArray
    }

    /**
    Gives you back a collection of User objects for the provided JSON Serialized Data.  This method handles both a single object (map) and an array of objects (array of maps).
    - Parameter data: The NSData (optional) to pull the objects from.
    - Returns: An array of User objects that were deserialized from the provided data.
    */
    class func fromData(data: NSData?) -> [User]? {
        guard let data = data else {
            return []
        }

        do {
            let jsonData = try NSJSONSerialization.JSONObjectWithData(data, options: .AllowFragments)

            // If we have an array of dictionaries:
            if let jsonArray = jsonData as? [[String:AnyObject]] {
                return fromArrayOfMaps(jsonArray)
            } else if let jsonMap = jsonData as? [String:AnyObject] {
                if let model = fromMap(jsonMap) {
                    return [model]
                }
            } else {
                print("Unknown JSON Data type: \(jsonData)")
            }
        } catch {
            print("Error deserializing json data")
        }

        return []
    }

	/**
	 Gives you back a collection of User objects after deserializing them from a String to Data, Data to a Dictionary and then finally to a Model Object.
	 - Parameter json: The (UTF-8) JSON String to deserialize into User objects.
	 - Returns: An Array of User objects.
	*/
	class func fromJson(json: String) -> [User]? {
		let data = (json as NSString).dataUsingEncoding(NSUTF8StringEncoding)
		return fromData(data)
	}

}

// MARK: - Date Methods

extension User {

    	class func fromEpochDateInt(dateInt: Int?) -> NSDate? {
    	    guard let dateInt = dateInt else {
    	        return nil
    	    }

    		let dateDouble = NSTimeInterval(Double(dateInt) / 1000)
    		return NSDate(timeIntervalSince1970: dateDouble)
    	}

    	class func toEpochDateInt(date: NSDate?) -> Int? {
    	    guard let date = date else {
    			return nil
    		}

    		return Int(date.timeIntervalSince1970 * 1000)
    	}

}
