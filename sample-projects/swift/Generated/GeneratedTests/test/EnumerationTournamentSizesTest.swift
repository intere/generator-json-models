/**
 * EnumerationTournamentSizes.swift
 *
 * Generated by JSON Model Generator v0.0.4 on Aug 13, 2016
 * https://github.com/intere/generator-json-models
 *
 * The generator tool is licensed under the LGPL: http://www.gnu.org/licenses/lgpl-3.0.html#content
 */

import XCTest
@testable import Generated

class EnumerationTournamentSizesTest: XCTestCase {

    override func setUp() {
        super.setUp()
    }

    override func tearDown() {
        super.tearDown()
    }
}

// MARK: - name Tests

extension EnumerationTournamentSizesTest {

    func testNameFromMap() {
        guard let model = EnumerationTournamentSizes.fromMap(["name": "You know how they say you only hurt the ones you love? Well, it works both ways."]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertEqual("You know how they say you only hurt the ones you love? Well, it works both ways.", model.name, "The name is incorrect")
    }

    func testNameFromEmptyMap() {
        guard let model = EnumerationTournamentSizes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertNil(model.name, "The name is not nil")
    }
}

// MARK: - size Tests

extension EnumerationTournamentSizesTest {

    func testSizeFromMap() {
        guard let model = EnumerationTournamentSizes.fromMap(["size": 4566688010606117888]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertEqual(4566688010606117888, model.size, "The size is incorrect")
    }

    func testSizeFromEmptyMap() {
        guard let model = EnumerationTournamentSizes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertNil(model.size, "The size is not nil")
    }
}

