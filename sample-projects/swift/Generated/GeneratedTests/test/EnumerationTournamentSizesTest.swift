/**
 * EnumerationTournamentSizesTest.swift
 *
 * Generated by JSON Model Generator v0.0.5 on Aug 28, 2016
 * https://github.com/intere/generator-json-models
 *
 * The generator tool is licensed under the LGPL: http://www.gnu.org/licenses/lgpl-3.0.html#content
 */

import XCTest
@testable import Generated

class EnumerationTournamentSizesTest: XCTestCase {

    override func setUp() {
        super.setUp()
    }

    override func tearDown() {
        super.tearDown()
    }
}

// MARK: - name Tests

extension EnumerationTournamentSizesTest {

    func testNameFromMap() {
        guard let model = EnumerationTournamentSizes.fromMap(["name": "I look at God behind his desk, taking notes on a pad, but God’s got this all wrong."]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertEqual("I look at God behind his desk, taking notes on a pad, but God’s got this all wrong.", model.name, "The name is incorrect")
    }

    func testNameFromEmptyMap() {
        guard let model = EnumerationTournamentSizes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertNil(model.name, "The name is not nil")
    }
}

// MARK: - size Tests

extension EnumerationTournamentSizesTest {

    func testSizeFromMap() {
        guard let model = EnumerationTournamentSizes.fromMap(["size": 2747348041408075776]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertEqual(2747348041408075776, model.size, "The size is incorrect")
    }

    func testSizeFromEmptyMap() {
        guard let model = EnumerationTournamentSizes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentSizes was nil")
            return
        }

        XCTAssertNil(model.size, "The size is not nil")
    }
}

