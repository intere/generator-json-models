/**
 * EnumerationTournamentTypesTest.swift
 *
 * Generated by JSON Model Generator v0.0.5 on Aug 28, 2016
 * https://github.com/intere/generator-json-models
 *
 * The generator tool is licensed under the LGPL: http://www.gnu.org/licenses/lgpl-3.0.html#content
 */

import XCTest
@testable import Generated

class EnumerationTournamentTypesTest: XCTestCase {

    override func setUp() {
        super.setUp()
    }

    override func tearDown() {
        super.tearDown()
    }
}

// MARK: - name Tests

extension EnumerationTournamentTypesTest {

    func testNameFromMap() {
        guard let model = EnumerationTournamentTypes.fromMap(["name": "At the time, my life just seemed too complete, and maybe we have to break everything to make something better out of ourselves."]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertEqual("At the time, my life just seemed too complete, and maybe we have to break everything to make something better out of ourselves.", model.name, "The name is incorrect")
    }

    func testNameFromEmptyMap() {
        guard let model = EnumerationTournamentTypes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertNil(model.name, "The name is not nil")
    }
}

// MARK: - value Tests

extension EnumerationTournamentTypesTest {

    func testValueFromMap() {
        guard let model = EnumerationTournamentTypes.fromMap(["value": 783560179519344640]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertEqual(783560179519344640, model.value, "The value is incorrect")
    }

    func testValueFromEmptyMap() {
        guard let model = EnumerationTournamentTypes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertNil(model.value, "The value is not nil")
    }
}

