/**
 * EnumerationTournamentTypes.swift
 *
 * Generated by JSON Model Generator v0.0.4 on Aug 13, 2016
 * https://github.com/intere/generator-json-models
 *
 * The generator tool is licensed under the LGPL: http://www.gnu.org/licenses/lgpl-3.0.html#content
 */

import XCTest
@testable import Generated

class EnumerationTournamentTypesTest: XCTestCase {

    override func setUp() {
        super.setUp()
    }

    override func tearDown() {
        super.tearDown()
    }
}

// MARK: - name Tests

extension EnumerationTournamentTypesTest {

    func testNameFromMap() {
        guard let model = EnumerationTournamentTypes.fromMap(["name": "I look at God behind his desk, taking notes on a pad, but God’s got this all wrong."]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertEqual("I look at God behind his desk, taking notes on a pad, but God’s got this all wrong.", model.name, "The name is incorrect")
    }

    func testNameFromEmptyMap() {
        guard let model = EnumerationTournamentTypes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertNil(model.name, "The name is not nil")
    }
}

// MARK: - value Tests

extension EnumerationTournamentTypesTest {

    func testValueFromMap() {
        guard let model = EnumerationTournamentTypes.fromMap(["value": 7250527194938281984]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertEqual(7250527194938281984, model.value, "The value is incorrect")
    }

    func testValueFromEmptyMap() {
        guard let model = EnumerationTournamentTypes.fromMap([:]) else {
            XCTFail("The EnumerationTournamentTypes was nil")
            return
        }

        XCTAssertNil(model.value, "The value is not nil")
    }
}

